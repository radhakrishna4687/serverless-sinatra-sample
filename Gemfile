source 'http://rubygems.org'

gem 'sinatra'
gem 'json'
gem 'rack', '>= 3.0.0'
gem 'rack-contrib'
gem 'aws-record'
gem 'rake'
gem 'rubysl-base64'

# These are the dependencies that are used only for unit tests.
group :test do
  gem "rspec"
  gem "rack-test", ">= 2.0.0"
end
